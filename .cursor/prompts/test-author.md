# Cursor test-author prompt

Mirror `.claude/agents/test-author.md`. Delta: execute the `buildPlan` directive fields
(`resolvedLayer`, `apiShape`, `renderMode`) as decided by the coordinator — do not re-derive them. See
`## Execute the directive — do not re-reason` in the agent file.
